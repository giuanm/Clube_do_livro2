<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Club</title>
    <link rel="shortcut icon" href="/img/Livros.png" type="image/x-icon">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
</head>
<body>
    <nav id="navbar">
        <a href="/" id="logo">
            <img src="/img/Livros.png" alt="Livros">
        </a>
        <h3 class="userNav">
            {{#if session.userid}}
                <span>@{{session.username}}</span>
            {{else}}
                <span>Welcome to the Book Club</span>
            {{/if}}
        </h3>
        <ul>
            <li><a href="/">Comments</a></li>
            <!-- URLs após autenticação -->
            {{#if session.userid}}
                <li><a href="/livros/dashboard">Dashboard</a></li>
                <li><a href="/logout">Logout</a></li>
            {{else}}
                <li><a href="/login">Login</a></li>
                <li><a href="/register">Register</a></li>
            {{/if}}
        </ul>
    </nav>
    <div class="container">
        {{#if messages.message}}
            <div class="message">
                {{messages.message}}
            </div>
        {{/if}}
        {{{body}}}
    </div>
    <footer>
        <p><span>Book Club</span> &copy; 2024</p>
    </footer>
</body>
</html>